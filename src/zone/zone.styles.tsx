import { StyleSheet } from "react-native";

export default StyleSheet.create({
    root: {
        flexDirection: 'row',
        alignItems: 'center',
        margin: 15
    },
    title: {
        fontWeight: 'bold'
    },
    titleContainer: {
        flex: 1
    }
});