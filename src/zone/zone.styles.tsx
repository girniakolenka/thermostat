import { StyleSheet } from "react-native";

export default StyleSheet.create({
    root: {
        flexDirection: 'row',
        alignItems: 'center',
        margin: 5
    },
    title: {
        fontWeight: 'bold'
    },
    titleContainer: {
        flex: 1
    }
});