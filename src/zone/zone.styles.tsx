import { StyleSheet } from "react-native";

export default StyleSheet.create({
    root: {
        flexDirection: 'row',
        alignItems: 'center'
    },
    title: {
        fontWeight: 'bold'
    }
});